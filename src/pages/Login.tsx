import { FC, Fragment, ReactElement } from 'react';

const Login: FC = (): ReactElement => {
  return (
    <Fragment>
      <h1>Login</h1>
      <div>
        <button>Login</button>
      </div>
    </Fragment>
  );
};

export default Login;
