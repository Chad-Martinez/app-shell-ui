import { Typography } from '@mui/material';
import { FC, ReactElement } from 'react';

const AdminHome: FC = (): ReactElement | null => {
  return (
    <Typography variant='h4' color='#524d4d' gutterBottom>
      Admin Home
    </Typography>
  );
};

export default AdminHome;
