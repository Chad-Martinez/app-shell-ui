import { Typography } from '@mui/material';
import { FC, ReactElement } from 'react';

const Admin2: FC = (): ReactElement | null => {
  return (
    <Typography variant='h4' color='#524d4d' gutterBottom>
      Admin 2
    </Typography>
  );
};

export default Admin2;
